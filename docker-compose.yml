services:
  service-discovery:
    build:
      context: ./service-discovery
      dockerfile: Dockerfile
    ports:
      - "8761:8761"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  api-gateway-service:
    build:
      context: ./api-gateway-service
      dockerfile: Dockerfile
    depends_on:
      - service-discovery
    environment:
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  user-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: users_db
      POSTGRES_USER: malik
      POSTGRES_PASSWORD: 12345678
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U malik -d users_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - user-db-data:/var/lib/postgresql/data
    networks:
      - booking-hotel-network

  user-service:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    depends_on:
      service-discovery:
        condition: service_started
      user-db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://user-db:5432/users_db
      SPRING_DATASOURCE_USERNAME: malik
      SPRING_DATASOURCE_PASSWORD: 12345678
      EUREKA_CLIENT_ENABLED: "true"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8081:8081"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  catalog-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: catalogs_db
      POSTGRES_USER: malik
      POSTGRES_PASSWORD: 12345678
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U malik -d catalogs_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - catalog-db-data:/var/lib/postgresql/data
    networks:
      - booking-hotel-network

  catalog-service:
    build:
      context: ./catalog-service
      dockerfile: Dockerfile
    depends_on:
      service-discovery:
        condition: service_started
      catalog-db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://catalog-db:5432/catalogs_db
      SPRING_DATASOURCE_USERNAME: malik
      SPRING_DATASOURCE_PASSWORD: 12345678
      EUREKA_CLIENT_ENABLED: "true"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8082:8082"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  booking-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: booking_db
      POSTGRES_USER: malik
      POSTGRES_PASSWORD: 12345678
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U malik -d booking_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - booking-db-data:/var/lib/postgresql/data
    networks:
      - booking-hotel-network

  booking-service:
    build:
      context: ./booking-service
      dockerfile: Dockerfile
    depends_on:
      service-discovery:
        condition: service_started
      booking-db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://booking-db:5432/booking_db
      SPRING_DATASOURCE_USERNAME: malik
      SPRING_DATASOURCE_PASSWORD: 12345678
      EUREKA_CLIENT_ENABLED: "true"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8083:8083"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  notification-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: notification_db
      POSTGRES_USER: malik
      POSTGRES_PASSWORD: 12345678
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U malik -d notification_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - notification-db-data:/var/lib/postgresql/data
    networks:
      - booking-hotel-network

  notification-service:
    build:
      context: ./notification-service
      dockerfile: Dockerfile
    depends_on:
      service-discovery:
        condition: service_started
      notification-db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://notification-db:5432/notification_db
      SPRING_DATASOURCE_USERNAME: malik
      SPRING_DATASOURCE_PASSWORD: 12345678
      EUREKA_CLIENT_ENABLED: "true"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8084:8084"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  payment-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: payment_db
      POSTGRES_USER: malik
      POSTGRES_PASSWORD: 12345678
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U malik -d payment_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - payment-db-data:/var/lib/postgresql/data
    networks:
      - booking-hotel-network

  payment-service:
    build:
      context: ./payment-service
      dockerfile: Dockerfile
    depends_on:
      service-discovery:
        condition: service_started
      notification-db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://notification-db:5432/payment_db
      SPRING_DATASOURCE_USERNAME: malik
      SPRING_DATASOURCE_PASSWORD: 12345678
      EUREKA_CLIENT_ENABLED: "true"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8085:8085"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  review-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: review_db
      POSTGRES_USER: malik
      POSTGRES_PASSWORD: 12345678
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U malik -d review_db" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - review-db-data:/var/lib/postgresql/data
    networks:
      - booking-hotel-network

  review-service:
    build:
      context: ./review-service
      dockerfile: Dockerfile
    depends_on:
      - service-discovery
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://notification-db:5432/review_db
      SPRING_DATASOURCE_USERNAME: malik
      SPRING_DATASOURCE_PASSWORD: 12345678
      EUREKA_CLIENT_ENABLED: "true"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8086:8086"
    restart: unless-stopped
    networks:
      - booking-hotel-network

  admin-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: admin_db
      POSTGRES_USER: malik
      POSTGRES_PASSWORD: 12345678
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U malik -d admin_db" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - admin-db-data:/var/lib/postgresql/data
    networks:
      - booking-hotel-network

  admin-service:
    build:
      context: ./admin-service
      dockerfile: Dockerfile
    depends_on:
      service-discovery:
        condition: service_started
      notification-db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://notification-db:5432/admin_db
      SPRING_DATASOURCE_USERNAME: malik
      SPRING_DATASOURCE_PASSWORD: 12345678
      EUREKA_CLIENT_ENABLED: "true"
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://service-discovery:8761/eureka
    ports:
      - "8087:8087"
    restart: unless-stopped
    networks:
      - booking-hotel-network

networks:
  booking-hotel-network:
    driver: bridge

volumes:
  user-db-data:
  catalog-db-data:
  booking-db-data:
  notification-db-data:
  payment-db-data:
  review-db-data:
  admin-db-data:
